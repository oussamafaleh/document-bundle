<form method="post" action="{{ path('user_item_property_delete', {'id': user_item_property.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_item_property.id) }}">
    <button class="btn btn-round btn-danger">Delete</button>
</form>
